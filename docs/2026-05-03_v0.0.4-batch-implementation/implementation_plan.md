# 実装計画

## 1. データモデル/永続化
- `VideoItem` に `locked`, `durationSeconds` を追加
- `GroupItem` に `locked`, `builtin` を追加
- `dataVersion` を 3 に更新
- ストア読み込み時にお気に入り固定グループを補完

## 2. ライブラリ/グループ共通ロジック
- `libraryHelpers` に以下を追加
  - お気に入り補完
  - 複数動画削除（ロック除外）
  - 複数グループ削除（ロック/固定除外）
  - 複数ロック
  - 動画長保存

## 3. Zustand ストア拡張
- ソート状態（キー/昇降順）を追加
- ライブラリ/グループ選択モード状態を追加
- 実行ログ配列と追加APIを追加
- お気に入り追加、選択削除/ロック、動画長保存アクションを追加

## 4. UI改修
- ライブラリ
  - 全件削除廃止
  - 選択モード UI とダイアログ
  - ソート UI（名前/長さ + 昇降順）
- グループ
  - 星ボタン（再生中動画をお気に入りへ）
  - 選択モード UI
- レイアウト
  - フッター廃止
  - 右サイド常設
  - キュー + 実行ログ表示
- アイコン
  - タブ/ダイアログ閉じるを `x` に統一
  - サイド折りたたみは既存矢印維持

## 5. プレイヤー改修
- 自動非表示を 3 秒に変更
- シークバー hover 時刻+静止画プレビュー
- シークバー focus 中でも左右/J/K 有効化
- 音量バー着色
- 再生時間表示 `user-select: none`
- `loadedmetadata` で動画長を保存

## 6. テスト
- `yarn typecheck`
- `yarn test:watch --run`
- 追加単体テスト
  - ロック削除保護
  - お気に入り固定維持
  - 日本語自然順ソート
  - 長さ未取得末尾ルール

# 実装計画

1. `src/web/components/AppToast.tsx` を新規作成する。
2. `App.tsx` のトースト直描画を `AppToast` 呼び出しへ置換する。
3. `index.css` のトースト関連スタイルを固定レイアウト（幅・高さ・列構成）へ変更する。
4. メッセージ部分を単一行省略（ellipsis）へ変更する。
5. `yarn typecheck` で確認する。

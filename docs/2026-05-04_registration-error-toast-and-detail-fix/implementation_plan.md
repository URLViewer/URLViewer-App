# 実装計画

1. App のログ監視ロジックを見直し、アプリ起動後「最初に発生したエラーログ」でもトースト表示されるようにする。
2. 登録処理の失敗結果に `reason` を保持するようワークフロー型を拡張する。
3. registrationSlice で失敗サマリーログに `detail` を追加し、ログパネルで詳細閲覧できるようにする。
4. `yarn typecheck` で型整合性を確認する。

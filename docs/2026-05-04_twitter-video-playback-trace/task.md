# タスク

- X/Twitter の mp4 URL がブラウザでは再生できるのに、アプリでは `HTMLMediaError code=4` で失敗する問題を調査・改善する。
- 失敗時にログパネルで原因を追えるよう、実再生リクエストの結果（HTTPステータス等）を詳細ログへ含める。
- 再生コンポーネントをブラウザ成功例（`<source type="video/mp4">`）に近づける。

# 実装計画

1. プレビュー取得の間引き（固定待機）を撤廃し、最新ターゲットを即時処理する。
2. 近接ターゲットのスキップ判定を外し、更新頻度を落とさない。
3. プレビュー動画のシークに `fastSeek` を利用可能なら使い、シーク完了までの時間を短縮する。
4. 追従アニメーション（left transition）を削除し、取得結果を遅延なく反映する。
5. `yarn typecheck` で確認する。

# 実装計画

1. 現状確認
- `build/icon.png` の解像度を取得する。

2. トリミング
- 白背景（しきい値付き）と透明領域を背景として扱い、前景の外接矩形を算出。
- 算出した範囲でクロップし、`build/icon.png` を更新。
- 元ファイルは `build/icon.before-trim.png` として保存。

3. 確認
- 更新後の解像度を確認する。
- ファイル更新時刻とサイズを確認する。

# 修正内容の確認

## 変更概要
- `build/icon.png` の白余白を自動トリミングした。

## 実施内容
- 前景矩形: `x=81, y=0, w=1746, h=1875`
- 出力ファイル:
  - 更新後: `build/icon.png`（1746x1875）
  - バックアップ: `build/icon.before-trim.png`（元の2048x2048）

## 確認結果
- `ffprobe` にて更新後解像度を確認: `1746x1875`
- 形式は `rgba` の PNG を維持。

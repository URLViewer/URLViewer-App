# 修正内容の確認

## 画質向上

- プレビュー用キャンバス解像度を `160x90` から `240x135` に変更。
- JPEG品質を `0.7` から `0.9` に変更。
- `imageSmoothingQuality = "high"` を有効化。

## 滑らかさ改善

- プレビュー取得をキュー化し、`85ms` 間隔で処理するように変更。
- 前回とほぼ同一ターゲット（時刻差/位置差が小さい）をスキップ。
- `mousemove` の都度即時キャプチャせず、最新ターゲットを優先して追従。
- 失敗時に既存プレビュー画像を消さないようにし、ちらつきを抑制。
- CSSで `left` の短いトランジションを付与。

## 付随調整

- プレビュー表示サイズを `180x102` に拡張し、視認性を改善。

## 検証

- `yarn typecheck` 成功。

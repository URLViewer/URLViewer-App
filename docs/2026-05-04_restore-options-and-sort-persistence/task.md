# タスク

- 動画ライブラリの並び替えキー/昇順降順を次回起動時に復元できるようにする。
- 設定パネルに「起動時に復元するもの」を追加する。
  - タブ
  - 再生していた動画
  - ライブラリの並び替え
- OFFにした項目は、終了時だけでなく通常保存時を含め、該当情報を保存ファイルへ書き込まないようにする（クライアント側で無視するだけにしない）。
- Electron操作が必要な箇所はIPC APIを経由させ、フロントとバックエンドの疎結合を維持する。

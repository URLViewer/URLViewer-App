# 実装計画

1. `GroupPanel` の上部操作エリアを1段から2段へ分割する。
2. 1段目に入力/追加/お気に入り追加、2段目に選択ボタンを配置して圧迫を軽減する。
3. `VideoPlayer` のシークプレビュー描画で、固定サイズキャンバスへ直接引き伸ばす処理を廃止する。
4. 元動画の縦横比を保持して中央配置で描画し、余白は黒で埋める。
5. CSS でプレビュー画像も `object-fit: contain` に変更して見た目の歪みを防ぐ。
6. `yarn typecheck` で確認する。

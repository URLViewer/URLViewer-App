# 実装計画

1. `GroupPanel` のタイトル表示を見直し、星アイコンとテキストを同一の `inline-flex` 行で揃える。
2. プレビュー生成処理の失敗条件を確認し、プレビュー用 `<canvas>` の存在有無とプレビュー動画初期化方法を修正する。
3. プレビュー用動画を本再生と同じプラグイン解決ロジックで初期化し、m3u8 でもサムネイル生成可能にする。
4. `yarn typecheck` で型破壊がないことを確認する。
